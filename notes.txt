window.localStorage
	hashbag
		collection of key value pair
	both key and value should be string
	API

	setItem(k,v)
	getItem(k) => v
	removeItem(k)
	key(ndex) => k
	clear() 

	length

Offline Application Cache

manifest
---------
CACHE MANIFEST
#VER 1.0

CACHE:
#List all the resources to be cached by the browser

NETWORK:
#List all the resources that should NOT be cached by the browser

FALLBACK:
#List offline alternatives for online resources